<h1>Categorías</h1>

<table class="table">
  <thead>
    <tr>
      <th>Nombre</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @property_categories.each do |property_category| %>
      <tr>
        <td><%= property_category.name %></td>
        <td><%= link_to 'Editar', edit_property_category_path(property_category) %></td>
        <td><%= link_to 'Eliminar', property_category, method: :delete, data: { confirm: 'Are you sure?' } %></td>
      </tr>
    <% end %>
  </tbody>
</table>

<br>

<%= link_to 'Crear Categoria', new_property_category_path, class: 'btn btn-default' %>
